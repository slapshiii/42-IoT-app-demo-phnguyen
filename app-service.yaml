apiVersion: v1
kind: Service
metadata:
  labels:
    app: app
  name: app
  namespace: dev
spec:
  ports:
  - nodePort: 32322
    port: 8888
    protocol: TCP
    targetPort: 8888
  selector:
    app: dev
  type: NodePort