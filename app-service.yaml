apiVersion: v1
kind: Service
metadata:
  labels:
    app: app
  name: app-service
  namespace: dev
spec:
  ports:
    - port: 8888
      targetPort: 8888
  type: LoadBalancer
  selector:
    app: app
